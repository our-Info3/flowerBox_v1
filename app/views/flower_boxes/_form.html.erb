
<%= form_for (@flower_box) do |f| %>
  <% if @flower_box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flower_box.errors.count, "error") %> prohibited this flower_box from being saved:</h2>

      <ul>
      <% @flower_box.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :size %><br>
    <%= f.number_field :size %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
